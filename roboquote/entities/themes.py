"""Enum to list all the different picture themes available."""
from enum import Enum


class Theme(Enum):
    """Enum to list all the different picture themes available."""

    MOUNTAIN = "mountain"
    SEA = "sea"
